package com.example.nhadanshop.entity;

/**
 * @deprecated Đã migrate sang Product(productType=COMBO) theo mô hình KiotViet.
 * Xem: Product.java, ProductComboItem.java
 * File này chỉ giữ để tránh compile error nếu có reference cũ.
 */
@Deprecated
public class ProductCombo {
    // Intentionally empty — replaced by Product(productType=COMBO)
}
